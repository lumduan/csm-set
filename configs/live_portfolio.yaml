# CSM-SET Live Test — Canonical Portfolio State
# ==============================================
# Single source of truth for the live paper-trading positions.  Used by
# ``csm.live.portfolio.load_live_portfolio`` to compute the actual NAV
# during ``run_post_refresh_hook`` (db_gateway.daily_performance and
# db_gateway.portfolio_snapshot).
#
# Update on each rebalance day (last trading day of the month for ATO on the
# first trading day of the next month).  Between rebalances this file is
# immutable.
#
# Symbols may be written either bare (``DELTA``) or prefixed (``SET:DELTA``);
# the loader prepends ``SET:`` when missing to match the parquet column
# format.
#
# avg_cost convention
# -------------------
# ``avg_cost`` is the **commission-inclusive average cost per share**, stored
# at 4 decimals.  This matches the broker's internal precision and lets the
# daily-log per-symbol ``%U.PL`` reproduce the broker statement exactly:
#
#     %U.PL = (shares * close_price) / (shares * avg_cost) - 1
#
# Brokers in Thailand display ``avg_cost`` rounded to 2 decimals on screen but
# compute ``%U.PL`` against the unrounded value.  When recording a fresh
# rebalance, take ``avg_cost`` from the broker's executed-trade confirmation
# (price + commission + VAT, divided by shares) rather than the on-screen
# rounded display.  The loader's NAV / drawdown math does not read
# ``avg_cost`` (only ``shares`` and ``cash``), so this field exists solely to
# drive the daily-log %U.PL calculation.

strategy_id: csm-set
# June 2026 rebalance (Option C — backtest top-10) executed at 2026-06-02 ATO.
# SELL: NEX, AGE, JTS.  BUY: KCE, EASTW, EPG.  MCOT (the 10th name) could not
# be filled on 2026-06-02 (liquidity too thin) and was deferred to 2026-06-04.
#
# 2026-06-04: MCOT partial fill (tranche 1 of 2 — thin liquidity).
#   BUY 8,100 MCOT @ 5.95 → gross 48,195.00 + fees @0.16799% 80.96 = all-in
#   48,275.96; avg_cost 5.9600 (commission-inclusive, 4-dp).  Remaining ~8,600
#   shares to fill on the next session (tranche 2).
#   Funded by a 100,000 THB fresh-capital injection (cash 11,307.27 + 100,000
#   − 48,275.96 = 63,031.31); the June-2 rebalance had left only 11,307.27 in
#   cash, so MCOT was never cash-funded — the injection covers it.
#   starting_nav bumped 1,000,000 → 1,100,000 so the injected capital is not
#   booked as a return; Total Return on NAV stays a true performance measure.
#   entry_date reset to 2026-06-04 (book composition + capital base both
#   changed) so the equity reconstruction restarts here and does not overwrite
#   the historical 06-02 equity row.
#
# 2026-06-05: MCOT tranche 2 of 2 — position complete at 16,700 shares.
#   BUY 8,600 MCOT @ 6.10 → gross 52,460.00 + fees @0.16799% 88.13 = all-in
#   52,548.13; cash 63,031.31 − 52,548.13 = 10,483.18.  Blended commission-
#   inclusive avg_cost over both tranches: (48,275.96 + 52,548.13) / 16,700 =
#   6.0374 (4-dp).  No capital injection — funded from the tranche-2 cash
#   reserve carried since 06-04; starting_nav unchanged at 1,100,000.
#   entry_date reset to 2026-06-05 because MCOT's share count changed: the
#   equity reconstruction reprices the *current* book back over
#   [entry_date, today] and UPSERTs every row, so leaving entry_date at 06-04
#   would overwrite the historical 06-04 equity row (1,119,768.31) with a
#   fictional 16,700-share mark.  Restarting at 06-05 preserves it.
#
# 2026-07-01: July rebalance — first fully systematic rotation (1-out/1-in),
# executed at ATO. Plan: docs/live-test/monthly/2026-06.md; rationale:
# docs/live-test/events/2026-06-30-rebalance-systematic-and-pipeline-gap.md.
#   SELL MCOT (16,700 sh @ 5.15) → gross 86,005.00 − fees @0.16799% 144.48 =
#   net 85,860.52; realized P/L vs cost basis 100,824.58 = −14,964.06
#   (−14.84%). MCOT was evicted on three independent grounds: buffer
#   eviction, EMA100 fast-exit (close 5.10 < EMA100 5.45 at the Jun 30
#   evaluation), and liquidity (63-day ADTV 3.58M THB below the 5M floor).
#   BUY FORTH (5,300 sh @ 16.10) → gross 85,330.00 + fees @0.16799% 143.35 =
#   all-in 85,473.35; avg_cost = 85,473.35 / 5,300 = 16.1270 (4-dp). FORTH
#   was the top eligible composite challenger (pct 0.947, ICT sector) and
#   steps directly into MCOT's old ICT sector slot.
#   Cash: 10,483.18 + 85,860.52 − 85,473.35 = 10,870.35.  No capital
#   injection — a clean, self-funding cash rotation.
#   entry_date reset to 2026-07-01 because the book composition changed: the
#   equity reconstruction reprices the *current* book back over
#   [entry_date, today], so it must restart here to avoid overwriting the
#   historical 06-05..06-30 equity rows with a fictional FORTH-instead-of-
#   MCOT mark.
entry_date: "2026-07-01"
starting_nav: 1100000.0
cash: 10870.35
positions:
  - symbol: DELTA
    shares: 300
    avg_cost: 319.5178
  - symbol: IRPC
    shares: 56600
    avg_cost: 2.3037
  - symbol: PTTGC
    shares: 3000
    avg_cost: 41.0672
  - symbol: HANA
    shares: 3000
    avg_cost: 35.8070
  - symbol: GUNKUL
    shares: 30800
    avg_cost: 2.9246
  - symbol: INSET
    shares: 42000
    avg_cost: 3.0848
  - symbol: KCE
    shares: 2600
    avg_cost: 39.0655
  - symbol: EASTW
    shares: 24000
    avg_cost: 4.2672
  - symbol: EPG
    shares: 17300
    avg_cost: 5.9099
  - symbol: FORTH
    shares: 5300
    avg_cost: 16.1270
